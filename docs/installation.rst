============
Installation
============

At the command line::

    $ easy_install django-admin-lightweight-date-hierarchy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-admin-lightweight-date-hierarchy
    $ pip install django-admin-lightweight-date-hierarchy
